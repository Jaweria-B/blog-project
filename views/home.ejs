<%- include('partials/header'); -%>

<h1>Home</h1>

<p> <%= startingContent %></p>

<% posts.forEach(function(obj) { %>
    <h1> <%= obj.title %> </h1>
    <p> 
        <!-- <% if ( obj.body.length > 99 ) { %>    
            <%= obj.body.substring(0, 100) + "..." %>    
        <% } else { %>
            <%= obj.body + "  "%> 
        <% } %> -->
        <%= obj.body.substring(0, 100) + "..." %>
        <a href="/posts/<%=obj.title%>">Read More</a>
    </p>
<% }) %>

<%- include('partials/footer'); -%>